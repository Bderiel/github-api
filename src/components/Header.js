import React from 'react';
import header from '../assets/jopwell.png';

const Header = () => (
  <div className="header flex">
    <img className="logo" src={header} alt="logo" />
  </div>
);

export default Header;
